import 'package:flutter/material.dart';
class WriteMassage extends StatelessWidget {
  const WriteMassage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
